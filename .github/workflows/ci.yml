name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Lint, typecheck, test, build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      # postinstall rebuilds better-sqlite3 against Electron; needs the Electron binary,
      # which npm downloads during install.
      - name: Install
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck renderer
        run: npx tsc -b

      - name: Typecheck electron
        run: npx tsc -p electron --noEmit

      - name: Format check
        run: npx prettier --check src electron

      - name: Test
        run: npm test -- --coverage

      - name: Build
        run: npm run build

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage
          retention-days: 7
